import firebase from "firebase/app";
import "firebase/auth";

export const firebaseConfig = {
  apiKey: -,
  authDomain: -,
  databaseURL: -,
  projectId: -,
  storageBucket: -,
  messagingSenderId: -,
  appId: -,
  measurementId: -,
};
